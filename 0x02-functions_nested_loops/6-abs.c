#include "main.h"

/**
 * _abs - Arrange the absolute value  of an integer
 * @t: the integer to be completed
 * Return: the absolute value
 */

int _abs(int t)
{
if (t >= 0)
return (t);
else
return (-t);
}
