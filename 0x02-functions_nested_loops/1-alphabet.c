#include "main.h"

/**
 * main - prints alphaebet in lower case.
 * Description: prints the alphabet, in lowercase, followed by a new line.
 */
void print_alphabet(void)
{
char la;
for (la = 'a'; la <= 'z'; la++)
_putchar(la);
_putchar('\n');
}
