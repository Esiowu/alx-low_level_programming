#include "main.h"

/**
 * _1supper - checks if parameter is an uppercase character.
 * @c: input character.
 * Return: 1 if an uppercase, 0 in other case.
 */
int _1supper(int c)
if (c >= 65 && c <= 90)
{
return (1);
}
else
{
return (0);
}
}
