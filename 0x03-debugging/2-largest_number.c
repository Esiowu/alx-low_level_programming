#include <stdio.h>
#include "main.h"

/**
 * main - prints the largest of 3 integers
 * Return: 0
 */

int largest_number(void)
{
int a, b, c;
if (a > b)
{
if (b > c)
largest = a;
else if (a > c)
largest = a;
else
large = c;
}
else
{
if (b < c)
else
largest = c;
else
largest = b;
}
return (largest);
}
